pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Starting Build'
                script{
                    try{
                        echo 'scriptint is running'
                        python3 app.py	
                        echo 'script running done'
                    }
                    catch (err){
                        echo 'Error catched'
                        currentBuild.result = 'FAILURE'
                        echo 'Error catched'
                        throw err
                    }
                }
            }
        }
    }
    post{
        failure {
            echo 'Build failed!!!!'
        }
    }
}

